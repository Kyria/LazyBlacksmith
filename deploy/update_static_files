#!/bin/bash

echo "[INFO] Create /static"
mkdir -p /static

echo "[INFO] Copy files in /static ..."
echo "# js files"
cp -fR /lb/lazyblacksmith/static/js  /static
echo "# css files"
cp -fR /lb/lazyblacksmith/static/css /static
echo "# img files"
cp -fR /lb/lazyblacksmith/static/img /static

echo "[INFO] Done"
