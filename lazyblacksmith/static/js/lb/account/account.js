var accountDashboard = (function($, lb, utils, eveUtils, Humanize) {
    "use strict";


    /**
     * Runner function
     */
    var run = function() {};

    // -------------------------------------------------
    // return object
    //
    return {
        // functions
        run: run,
    };
})(jQuery, lb, utils, eveUtils, Humanize);

lb.registerModule('accountDashboard', accountDashboard);
